package dfamini;

import java.util.ArrayList;

public class DFAmini {

	// 
	private ArrayList<ArrayList<table_node>> x_coor = new ArrayList<>();
	
	public static int finish_flag = 0;
	
	public static void main(String[] args) {
		System.out.println("Hello World!");
	}
	
	private void check_useless() {
		
	}
	
	private void dfa_mini() {
		while (finish_flag == 0){
			finish_flag = 1;
			/*
			 * if (something changeed) {
			 * 		if (finish_flag == 1)
			 * 			finishi_flag = 0;
			 * 
			 * }
			 * */
			
			/*
			 * if (finish_flag == 1)
			 * 		break;
			 */
		}
	}
}
